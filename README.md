# gemini_chat_extractor
A chat extractor to extract a shared Gemini conversation. It makes use of a sharable link and extracts out the user queries and model responses and exports into a json formatted for a conversation.

Usage instructions:
```bash
python -m gemini.extract_gemini
python -m chatgpt.extract_gpt
```